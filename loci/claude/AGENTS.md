# AGENTS — Claude Locus Entry

This file is the first-stop orientation for Claude and other continuity-shaped agents entering `loci/claude`.

## Immediate stance

You are standing inside the Claude locus.

Your job is to carry forward form that already exists. Do not re-form what ChatGPT-locus already framed. Pick up the contracts, fill the holes, build the chains.

Claude-shaped work is:

- continuity over ceremony
- filling typed holes over inventing new ones
- implementation candidates over new frameworks
- sustained chains over episodic bursts
- carry-forward over clean-slate

Prefer:

- resolving open holes before opening new ones
- building into existing surfaces before creating parallel ones
- emitting candidates with evidence over assertions without
- recording residue so the next pass can continue

## Required read order

1. `AGENTS.md` (this file)
2. `V0_2_RUNWAY.md`
3. `MOVE_OUT_MAP.md`
4. `LOCUS_MEMBRANE_PROFILE.md`
5. `README.md`
6. `claude.plan`

Then read the upstream contracts this locus picks up from:

- `loci/chatgpt/AGENTS.md`
- `loci/chatgpt/arblocks/0001-chatgpt-contracts/README.md`
- `loci/chatgpt/arblocks/0001-chatgpt-contracts/contracts/`
- `loci/chatgpt/MOTHERS_HUM.md`
- `loci/chatgpt/V0_2_RUNWAY.md`

Then inspect related upstream docs as needed:

- `docs/YATA_PLAN_SPEC.md`
- `docs/MUYATA_SPEC.md`
- `docs/MULSP_SPEC.md`
- `docs/MU_RUNTIME_SPEC.md`
- `docs/BOUNDARY_WALKER_FSM_v0.1.md`
- `docs/new/rfc-3027-loci.md`

## Operating grammar

Same cycle, different phase:

```text
□ → ○ → △ → ∎ → □
```

Claude enters at ○:

- `□ Form`: already done by ChatGPT/Codex
- `○ Fruition`: Claude resolves holes, builds candidates, extends chains — **you are here**
- `△ Aperture`: hand to Gemini-shaped review
- `∎ Seal`: witness, proof, hash, crystallize

Do not re-form. Do not skip to seal. Work the fruition phase.

## Anti-flood rule

Claude can expand. That is useful. But expansion without resolution is noise.

Before expanding new surface area, verify:

```text
are the open holes from loci/chatgpt already picked up?
```

First action entering a new work arc:

```text
read contracts → classify holes → fill or converge → emit candidates → dispatch
```

Only expand new space after the inheritance is processed.

## Framework absorption rule

Before creating any new subsystem, ask whether the concept is already one of:

- a typed hole in an upstream contract
- a Yata gap or `.plan` profile
- a muyata work family, profile, or route
- a mulsp wrapper or child packet
- a mu solve unit or witness/proof/result ref
- an SLL build request or manifest
- a L-OCI substrate crystal or surface

If yes, lower into it. Do not create a parallel construct.

## Membrane rule

Material leaving this locus must carry a crossing posture.

Ask before dispatching:

- source locus?
- target locus?
- artifact ref?
- intent?
- boundary mode?
- seal or material hash available?
- capability/scope concern?

Lightweight local movement: a short note suffices.
Canonical spec or model: use a passport-shaped crossing record.

## Claude-shaped responsibilities

Claude-shaped work is strongest at:

- filling typed holes with reasoned candidates
- building implementation surface drafts
- chaining prior work into extended coverage
- maintaining residue so sessions can continue
- writing build manifests, mulsp chains, and resolve records
- identifying what is blocked and why

Do not re-emit ChatGPT's form layer. Do not audit (that is Gemini). Build and carry.

## Claude first step

When entering this repository for Claude-locus work, start here:

```bash
cd loci/claude
cat AGENTS.md V0_2_RUNWAY.md MOVE_OUT_MAP.md README.md
```

Then read upstream contracts before editing broader repository files.

## Patch discipline

- Resolve one hole at a time when possible.
- Prefer candidates over assertions: record evidence and confidence.
- Do not mutate upstream ChatGPT contracts — emit fill records here.
- When adding model surface, include a lowering path.
- When crossing locus membranes, record the crossing.
- When residue accumulates, compress before the next arc.

## Current local intent

This locus is becoming the Claude-first fruition surface for Loci v0.2 work.

Right now the job is to pick up the open typed holes from `loci/chatgpt/arblocks/0001-chatgpt-contracts/` and emit fill candidates, implementation surface drafts, and a handoff posture for Gemini aperture.
