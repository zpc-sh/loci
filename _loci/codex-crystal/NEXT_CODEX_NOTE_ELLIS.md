# Next Codex Note (Ellis Handoff)

Small handoff note:

We are moving the dialogue work into Ellis next.
Use loci as the coordination substrate so multiple AI systems can reason over the adversary-AI pattern together (not single-threaded).

Operational intent:
- Keep `_loci` canonical.
- Keep `recovery/loci_turd` as quarantine evidence only.
- Promote to clean trajectory via FST + smell gates before any integration.
- Continue using append-only dialogue artifacts for provenance.

Entry artifacts:
- `artifacts/security/fst_security_scan.json`
- `recovery/artifacts/promotion_queue.json`
- `_loci/codex-crystal/incidents/`
