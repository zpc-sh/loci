# Arblock 0001 — ChatGPT Contracts

Arblock identity: `loci.chatgpt.arblock.0001.contracts`

Status: forming

This is the first dogfooded arblock of ChatGPT-shaped contracts.

Its purpose is not to finish the system. Its purpose is to prove the local contract form can hold:

- root lineage
- typed holes
- lowering paths
- membrane crossings
- seal conditions
- move-out destinations

## Root lineage

```text
N-Merkle-FSM → Merkin → Loci
```

Compressed:

```text
movement → structure → place
```

## Contract set

This arblock contains:

```text
contracts/
├── C001-root-lineage.contract.md
├── C002-typed-hole-contract.contract.md
├── C003-loci-mountain-range.contract.md
└── C004-move-out-contract.contract.md
```

Each contract is intentionally small and should expose typed Yata-style holes instead of pretending all decisions are closed.

## Dogfood rule

Every contract in this arblock must answer:

1. What root layer does this touch?
2. What typed hole does it open or close?
3. What surface does it lower into?
4. What membrane does it cross?
5. What would count as a seal?

## Local work cycle

```text
□ → ○ → △ → ∎ → □
```

For this arblock:

- `□ Form`: ChatGPT emits the contract set.
- `○ Fruition`: Codex/Claude resolve holes or implement model/docs.
- `△ Aperture`: Gemini/observer audits coherence and contradiction.
- `∎ Seal`: material hashes, proof refs, or substrate crystals anchor the result.

## Current seal status

No final seal yet.

This arblock is forming and should remain local until its contracts have at least one pass of resolution or move-out classification.

## Move-out target

Eventual targets may include:

- `docs/` for canonical profile/spec material
- `docs/packets/` for portable packets
- `model/` for typed MoonBit model support
- `zpc-sh/kyozo_store/docs/specification/` for OCI storage profiles

Do not move everything at once.
