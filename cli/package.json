{
  "name": "@zpc/loci",
  "version": "0.1.0",
  "description": "L-OCI content-addressed locus CLI/TUI (host-side storage surface for genius/loci)",
  "type": "module",
  "bin": { "loci": "./bin/loci" },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outfile dist/loci --target bun --minify",
    "wasm": "cd .. && moon build --target wasm --package zpc/genius/wasm_entry",
    "link": "bun link",
    "test": "bun test",
    "spec": "bun run src/index.ts spec",
    "spec:md": "bun run src/index.ts spec --format markdown",
    "spec:site": "bun run src/index.ts spec --format markdown --out ../docs/CLI_SPEC_v0.1.md",
    "demo:init": "bun run src/index.ts init",
    "demo:status": "bun run src/index.ts status"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
